

不要用大模型执行，大模型骂人都要花 Token：

同步网站的公共仓库目录 和同步 Obsidian 的私有目录：

`cd ~/self-learning-is-a-skill && git add -A && git commit -m "update" && git push`
`cd ~/Knowledge/ObsidianVault && git add -A && git commit -m "update" && git push`
